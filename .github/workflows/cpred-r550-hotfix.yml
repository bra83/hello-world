name: Cyberpunk RED R5.50 legacy hotfix (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded workflow guard
        shell: bash
        run: |
          echo "R5.50 is retained only as historical lineage."
          echo "Active Cyberpunk RED validation is .github/workflows/cpred-r549-fix.yml (R5.66+)."
          echo "No release is created from this legacy workflow."
